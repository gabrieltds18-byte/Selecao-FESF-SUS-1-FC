# Dependências do backend — instale com: pip install -r requirements.txt

fastapi==0.111.0    # framework web para criação da API REST
uvicorn==0.29.0     # servidor ASGI para rodar o FastAPI
pydantic==2.7.1     # validação e tipagem dos dados recebidos pela API
