// Arquivo de configuração do Next.js
// Permite personalizar o comportamento do framework:
// redirecionamentos, variáveis de ambiente, otimizações de imagem, etc.
// Por enquanto usa configuração padrão — pronto para expansão futura

/** @type {import('next').NextConfig} */
const nextConfig = {};

module.exports = nextConfig;
